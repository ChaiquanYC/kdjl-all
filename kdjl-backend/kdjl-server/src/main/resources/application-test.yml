spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kdjl_test?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: kdjl_test
    password: kdjl_test_pass

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.kdjl: TRACE
